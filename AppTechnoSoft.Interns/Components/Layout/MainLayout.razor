@inherits LayoutComponentBase

<MudThemeProvider IsDarkMode="isDarkMode" />
<MudDialogProvider />
<MudSnackbarProvider />
<MudPopoverProvider />

<MudLayout>
    <MudAppBar Elevation="1" Style="@("background:" + Colors.Teal.Lighten1)">
        <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start" OnClick="@((e) => DrawerToggle())" />
        <MudImage Src="/apptechnosoft.png" Alt="App Technosoft" Height="50" />
        <MudText Typo="Typo.h6" Align="Align.Center" Class="ml-2">APP TECHNOSOFT</MudText>
        <MudSpacer />
        <MudToggleIconButton @bind-Toggled="@isDarkMode"
                             Icon="@Icons.Material.Sharp.DarkMode" Size="Size.Large"
                             ToggledIcon="@Icons.Material.Sharp.LightMode" />

        <MudHidden Breakpoint="Breakpoint.Xs">
            <Login />
        </MudHidden>
    </MudAppBar>
    <MudDrawer @bind-Open="_drawerOpen" Elevation="2" ClipMode="DrawerClipMode.Always">
        <NavMenu />
    </MudDrawer>
    <MudMainContent Class="px-3 mt-3">
        @Body

        <MudToolBar Gutters="false" Class="mt-16 px-1 border-t-2 border-solid mud-border-tertiary" Dense>
            <MudText Typo="Typo.subtitle2" Style="font-size:1rem">@DateTime.Now.Year &copy; APP TECHNOSOFT</MudText>
            <MudSpacer />
            <MudText Typo="Typo.subtitle2" Style="font-size:1rem">With Love ❤️</MudText>
        </MudToolBar>
    </MudMainContent>
</MudLayout>

<div id="blazor-error-ui">
    An unhandled error has occurred.
    <a href="" class="reload">Reload</a>
    <a class="dismiss">🗙</a>
</div>

@code {
    bool _drawerOpen = false;
    bool isDarkMode = false;

    void DrawerToggle()
    {
        _drawerOpen = !_drawerOpen;
    }
}